import { defineMessages } from 'react-intl'

export const editor = defineMessages({
  pickupSelector: { title: 'admin/editor.pickup-selector.title' },
  zipcodeInput: { title: 'admin/editor.pickup-selector.zipcode-input.title' },
  skuSelector: { title: 'admin/editor.pickup-selector.sku-selector.title' },
})

export const zipcodeInput = defineMessages({
  label: 'pickup-selector.zipcode-input.label',
})

export const skuSelector = defineMessages({
  label: 'pickup-selector.sku-selector.label',
})
